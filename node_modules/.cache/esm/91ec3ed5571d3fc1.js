_571‍.x([["default",()=>_571‍.o]]);const mongoose = require('mongoose');

const kuponSchema = mongoose.Schema({
    kod: {
        type: String,
        
    },
    used: {
        type: Boolean,
        default: false
    },
    owner: {
        type: mongoose.Schema.Types.ObjectId, ref: 'kuponMusteri',  
        
    }
});

const Kupon = mongoose.model('Kupon', kuponSchema);
_571‍.d(Kupon);